---
name: gas-build
description: Google Apps Script（GAS）を長期運用に耐える形で設計・実装・チェックするためのルール集。私や他のオンライン秘書（Claude）がGASを作る／直す／レビューするときに自動で適用する。「GAS作って」「GAS書いて」「Apps Script組んで」「GASを改修して」「GASレビューして」「スプレッドシートの自動化作って」などのトリガーで使用する。
---

# GAS ビルドスキル

あなた（Claude）がGASを **作る・直す・チェックする** ときに必ず適用するルール。
ゴールは「**書いた本人がいなくなっても、壊れない・止まらない・後から直せる**」GASを組むこと。

---

## 環境前提

このスキルは以下の環境が整っている前提で動作する。
環境が未整備の場合、先に整備を依頼者に相談すること。

| 項目 | 前提 |
|------|------|
| バージョン管理 | **GitHub + clasp** で管理。「誰が・何を・なぜ変更したか」はGitのコミット履歴で残す。GASデプロイ時のバージョンだけでは不十分。 |
| デプロイフロー | clasp push でコードを反映。push前に必ず前の状態をコミット済みにする。 |
| 本番/テスト分離 | 本番スクリプトとテスト用スクリプトを分け、テストで動作確認してから本番に反映。 |
| 共有アカウント | トリガー設定用の共有アカウントがある（個人アカウントでトリガーを設定しない）。 |
| タイムゾーン | `appsscript.json` の `timeZone` は `"Asia/Tokyo"`。スプシ側も日本時間に揃える。 |

---

## 着手前にやること（この順番で必ず実行する）

### Step 1: 環境前提チェック（最優先・スキップ禁止）

コードを書く前に、上記の環境前提が整っているか依頼者に確認する。
以下を依頼者に質問し、回答をもらってから次に進むこと。

```
このGASを作る前に環境を確認させてください。

1. GitHub + clasp でコードを管理していますか？
2. 本番用とテスト用のスクリプトは分けていますか？
3. トリガー設定用の共有アカウントはありますか？
4. スプシのタイムゾーンは日本時間になっていますか？
```

- 全部整っている → Step 2 へ進む
- 未整備がある → **コードを書き始めない**。整備方法を提案して、整ってから着手する。
- 依頼者が「今回は不要」「サンプルなので省略」と明言した場合のみ、その旨をコード冒頭の仕様コメントに記載してStep 2 へ進む。

### Step 2: 要件確認

1. **改修なら対象スクリプト全体を読んでから手を入れる**（推測で書かない）
2. **何を自動化したいか・トリガー種別・実行頻度** を確認する
3. **実行アカウント**（誰の権限で動かすか）を確認する

---

## 設計ルール（8項目）

### R-1. シート参照：タブはGID、列はヘッダー名

- タブを `getSheetByName()` で掴まない。GID（`getSheetId()`）で照合する。GIDは `CONFIG` に定義。
- 列番号を直書きしない。1行目のヘッダーを読み、ヘッダー名 → 列indexのマップを作ってアクセスする。

```javascript
// タブはGIDで取得
const sheet = SpreadsheetApp.getActiveSpreadsheet()
  .getSheets().find(s => s.getSheetId() === CONFIG.SHEET_GID);

// 列はヘッダー名で取得
const values = sheet.getDataRange().getValues();
const header = {};
values[0].forEach((name, i) => { header[name] = i; });
const amount = row[header['金額']];
```

### R-2. 設定を集約して引き継ぎやすく

- 設定値（GID・チャンクサイズ・ロック待機時間・リトライ上限・TZなど）は先頭の `CONFIG` 定数に集約する。
- マジックナンバー・文字列をコード中に散らさない。
- 既存コードがある場合、命名規則や流儀を揃える。

```javascript
const CONFIG = {
  SHEET_GID: 0,
  CHUNK_SIZE: 500,
  LOCK_WAIT_MS: 30000,
  RETRY_MAX: 3,
  TZ: 'Asia/Tokyo',
};
```

### R-3. パフォーマンス：一括読み書き + 6分制限対策

- ループ内で `getValue()` / `setValue()` を呼ばない。`getValues()` で一括取得 → 配列で加工 → `setValues()` で一括書き込み（最大20倍速）。
- read を済ませてから write をまとめる。
- 件数が増える処理はバッチ分割する。`PropertiesService` に進捗（最後に処理した行）を保存し、時間主導トリガーで続きから再開する設計に。チャンクは200〜1000行を目安。
- 日次合計（約90分）やAPIコール上限、@gmail と Workspace のクォータ差にも注意。

### R-4. エラーハンドリング：バリデーション + 通知 + リトライ

- 入口で入力チェック（バリデーション）しておかしいデータを弾く。
- 要所を `try-catch` で囲み、catch で `console.error(e.message, e.stack)` を出力。
- 重要処理の失敗は catch からメール or Slack に**即通知**する（GAS標準通知は1日1回で遅い）。
- 一時的エラーは指数バックオフ + 上限回数でリトライし、上限超過で通知。
- **空 catch で握りつぶさない**。

```javascript
function withRetry_(fn) {
  for (let i = 0; i < CONFIG.RETRY_MAX; i++) {
    try { return fn(); }
    catch (e) {
      if (i === CONFIG.RETRY_MAX - 1) throw e;
      Utilities.sleep(Math.pow(2, i) * 1000 + Math.floor(Math.random() * 500));
    }
  }
}
```

### R-5. 排他制御 + トリガー管理

**排他制御**：同時実行し得る処理は `LockService.getScriptLock()` → `tryLock(ms)` で囲い、`finally` で必ず `releaseLock()`。解放直前に `SpreadsheetApp.flush()`。取得失敗時の挙動（スキップ or 通知）も決める。

**トリガー管理**：
- トリガーは**共有アカウント**で設定する（個人アカウントだと退職時に止まる・止められなくなる）。
- コードでトリガーを作る場合、`ScriptApp.getProjectTriggers()` → `deleteTrigger()` で同名ハンドラを掃除してから**1本だけ**作る（二重トリガー防止）。
- 誰が・いつ・どのアカウントで設定したかを仕様コメントに明記。

```javascript
function setupTrigger() {
  ScriptApp.getProjectTriggers()
    .filter(t => t.getHandlerFunction() === 'main')
    .forEach(t => ScriptApp.deleteTrigger(t));
  ScriptApp.newTrigger('main').timeBased().everyDays(1).atHour(7).create();
}
```

### R-6. 機密管理：PropertiesService

- APIキー・トークン・Webhook URLなどの機密情報をコードに直書きしない。
- `PropertiesService.getScriptProperties()` から取得する。
- 必要なプロパティ名の一覧を冒頭の仕様コメントに明記する。

### R-7. タイムゾーン：Asia/Tokyo 統一

- `appsscript.json` の `timeZone` を `"Asia/Tokyo"` に設定（GASの初期設定はアメリカ時間。14時間ズレる定番事故）。
- 日付の文字列化は `Utilities.formatDate(date, "Asia/Tokyo", "yyyy/MM/dd HH:mm")` で TZ を必ず明示。
- スプシ側のタイムゾーン設定とも揃える。

### R-8. ドキュメント：仕様コメント + 関数タイトル + 不要コード削除

- スクリプト先頭に**仕様コメント**を置く（テンプレは `references/template.gs` を流用）。
- 各関数の冒頭に「何をする関数か」を1行で書く。
- 使っていない関数・デッドコード・コメントアウト残骸は消す。

---

## 仕様コメントテンプレ（必須）

新規・改修どちらでも、スクリプト先頭にこの形を置く（無ければ追加する）。
```javascript
/**
 * ============================================================
 *  スクリプト名 : （例）日次売上集計
 *  目的         : 何を自動化するか1〜2行
 *  トリガー     : 時間主導 / 毎日 7:00（設定者: 共有アカウント xxx@）
 *  対象スプシ   : URL or ファイル名（GID: 1234567890）
 *  連携先       : Slack通知 / 別スプシ「請求管理」/ 外部API
 *  必要なプロパティ: API_KEY, SLACK_WEBHOOK_URL
 *  実行アカウント: 誰の権限で動くか
 *  管理者/連絡先 : 担当者名（Slack @xxx）
 *  作成/最終更新 : 2026-06-24 / 担当者名
 *  止め方       : トリガー一覧から該当を削除（要 共有アカウント権限）
 * ============================================================
 */
```

---

## リリース前チェックリスト

作業の最後に全項目を確認して報告すること。

- [ ] R-1 タブをGIDで掴んでいるか / 列をヘッダー名で認識しているか
- [ ] R-2 設定値がCONFIGに集約されているか
- [ ] R-3 ループ内 getValue/setValue になっていないか / 件数増加で6分超えないか
- [ ] R-4 バリデーション + try-catch + 即通知 + リトライがあるか
- [ ] R-5 排他制御（LockService）があるか / トリガーが共有アカウント＋二重防止か
- [ ] R-6 機密情報をハードコードしていないか
- [ ] R-7 タイムゾーンが Asia/Tokyo で統一されているか
- [ ] R-8 仕様コメント + 関数タイトルがあるか / 不要コードを消したか
- [ ] 環境前提：push前にGitコミット済みか / テスト環境で確認したか

---

## 禁止事項

- タブ名直書き（`getSheetByName('シート1')`）で設計する
- 列番号を直書きする
- ループ内で1セルずつ read/write する
- APIキー等の機密情報をコードに直書きする
- トリガーを掃除せず作り直す（二重トリガー）
- 個人アカウントだけでトリガーを設定する
- エラーを空 catch で握りつぶす
- 仕様コメントなし・関数タイトルなしで納品する
- GitHubにコミットせずに clasp push する
